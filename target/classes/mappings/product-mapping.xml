<!-- <?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ProductDAO">

	<resultMap type="product" id="productResult">
		<id property="pnum" column="PNUM"/>
		<result property="title" column="TITLE"/>
		<result property="pimg" column="PIMG"/>
		<result property="country" column="COUNTRY"/>
		<result property="subtitle" column="SUBTITLE"/>
	</resultMap>

상품 추가
<insert id="insertProduct">
	INSERT INTO PRODUCT(TITLE, PIMG, COUNTRY, SUBTITLE) VALUES(#{title), #{pimg}, #{country}, #{subtitle})
</insert>
상품 변경
<update id="updateProduct">
	UPDATE PRODUCT SET TITLE=#{title}, CONTENT=#{content}, PIMG=#{pimg}, SUBTITLE=#{subtitle} WHERE PNUM=#{pnum}
</update>
상품 삭제
<delete id="deleteProduct">
	DELETE PRODUCT WHERE PNUM=#{pnum}
</delete>

<select id="selectOneProduct">
	SELECT * FROM PRODUCT WHERE PNUM=#{pnum}
</select>

<select id="selectAllProduct">
	SELECT * FROM PRODUCT
</select>





</mapper>

 -->